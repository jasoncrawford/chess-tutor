# Hosted Coaching Interactions Plan

1. Add failing Python and Swift contract tests for `expects`, negative-answer actions, and strict decoding.
2. Add failing hosted session/projector integration tests for selection answers, local selections, and **Everything looks safe**.
3. Implement the minimal server/compiler/prompt and iPad contract/session/projector changes.
4. Run focused tests, full CI-equivalent verification, and direct simulator interaction checks.
5. Commit, push, and open a PR targeting `codex/chess-coaching-comparison`.
